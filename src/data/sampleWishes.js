// ---- EDIT DEFAULT / SAMPLE WISHES HERE ----
// These are shown the first time the site loads (before any real visitor
// has submitted a wish). Add entries here to seed the wall - each needs a
// unique id, name, message, and createdAt (ISO date string). Left empty on
// purpose so the live wall only shows genuine messages.
const sampleWishes = []

export default sampleWishes
